# Описание

**Rice & Stripes** — это веб-сайт о содержании амадин. Проект создан с использованием современных веб-технологий.

Сайт включает:
- 📚 **Статьи** — подробные материалы об уходе за птицами, их анатомии, питании и поведении
- 🧭 **Навигацию** — боковое меню с активным отслеживанием текущей страницы
- 🖼️ **Галереи изображений** — карусели с подписями для наглядного представления материала
- 📱 **Адаптивный дизайн** — сайт корректно отображается на разных устройствах

## Технологии

- **Next.js 16** — фреймворк для создания React-приложений
- **React 19** — библиотека для построения интерфейсов
- **TypeScript** — типизированный JavaScript для надежности кода
- **SCSS** — расширенный CSS для удобной стилизации
- **Bulma** — CSS-фреймворк для быстрой верстки

## Запуск проекта

### Требования

- **[Node.js](https://nodejs.org/)** версии 20 или новее
- **npm** или другой пакетный менеджер (yarn, pnpm)

### Установка зависимостей

Откройте терминал в папке проекта и выполните команду:

```bash
npm ci
```

Эта команда загрузит и установит все необходимые библиотеки точно в тех версиях, которые указаны в `package-lock.json`. Используйте `npm ci` для чистой установки или `npm install` для обновления зависимостей.

### Запуск в режиме разработки

После установки зависимостей запустите сервер разработки:

```bash
npm run dev
```

Сайт будет доступен по адресу [http://localhost:3000](http://localhost:3000)

В режиме разработки:
- ✨ Изменения в коде автоматически отображаются в браузере
- 🔍 Видны подробные сообщения об ошибках
- ⚡ Включена быстрая сборка (Turbopack)

## Структура проекта

```
next-test/
├── public/                     # Статические файлы (favicon, robots.txt)
├── src/
│   ├── app/                    # Приложение Next.js (App Router)
│   │   ├── (pages)/            # Группа маршрутов для страниц
│   │   │   ├── articles/       # Страницы статей
│   │   │   ├── home/           # Главная страница
│   │   │   ├── about/          # Страница "О проекте"
│   │   │   └── notes/          # Страница "Заметки"
│   │   ├── layout.tsx          # Корневой layout со Sidebar
│   │   ├── page.tsx            # Корневая страница (редирект на /home)
│   │   ├── globals.scss        # Глобальные стили
│   │   └── metadata.ts         # Общие метаданные сайта
│   │
│   ├── components/             # React-компоненты используемые на разных страницах
│   │
│   ├── models/                 # TypeScript типы и модели данных
│   │
│   ├── shared/                 # Общие ресурсы
│   │   ├── assets/             # Изображения статей и иконки
│   │   ├── styles/             # SCSS переменные и общие стили
│   │   └── metadata/           # Функции для создания метаданных
│   │
├── eslint.config.mjs           # Настройки ESLint
├── next.config.ts              # Конфигурация Next.js
├── tsconfig.json               # Конфигурация TypeScript
├── package.json                # Зависимости и скрипты
└── README.md                   # Документация проекта
```

### Структура `src/app/(pages)/`

Папка `(pages)` — это группа маршрутов в Next.js. Скобки означают, что название папки не влияет на URL.

```
src/app/(pages)/
├── articles/                   # Раздел статей
│   ├── page.tsx                # Список всех статей (/articles)
│   ├── metadata.ts             # Метаданные страницы списка
│   │
│   ├── anatomy/                # Статья об анатомии
│   │   ├── page.tsx            # Точка входа
│   │   ├── metadata.ts         # Метаданные и articleInfo
│   │   └── article.tsx         # Содержимое статьи
│   │
│   ├── letFinchesFly/          # Статья о полетах
│   ├── cageSelection/          # Статья о выборе клетки
│   ├── topTenMistakes/         # Статья об ошибках новичков
│   └── zebraFinchesNatureDiet/ # Статья о питании
│
├── home/                       # Главная страница
│   ├── page.tsx                # Точка входа (/home)
│   ├── metadata.ts             # Метаданные
│   └── content.tsx             # Содержимое страницы
│
├── about/                      # Страница "О проекте"
│   ├── page.tsx                # Точка входа (/about)
│   ├── metadata.ts             # Метаданные
│   └── about.tsx               # Содержимое страницы
│
└── notes/                      # Страница "Заметки"
    ├── page.tsx                # Точка входа (/notes)
    ├── metadata.ts             # Метаданные
    └── content.tsx             # Содержимое страницы
```

**Ключевые особенности:**

- **Каждая папка = отдельный маршрут** — Next.js автоматически создает URL на основе структуры папок
- **page.tsx обязателен** — это файл, который делает маршрут доступным
- **metadata.ts** — содержит SEO-данные и информацию о странице/статье
- **article.tsx / content.tsx** — фактическое содержимое для лучшей организации кода

## Разработка проекта

### Создание новых статей

Для добавления новой статьи необходимо создать подпапку в `src/app/(pages)/articles/` с тремя файлами:

#### Структура папки статьи

```
src/app/(pages)/articles/
└── myNewArticle/
    ├── page.tsx
    ├── metadata.ts
    └── article.tsx
```

#### 1. `page.tsx` — точка входа для Next.js

Этот файл экспортирует компонент страницы и метаданные для Next.js App Router.

```tsx
import type { FC } from "react";

import Article from "./article";
export { metadata } from "./metadata";

const MyArticlePage: FC = () => {
    return <Article />;
};

export default MyArticlePage;
```

**Зачем нужен:** Next.js использует этот файл для маршрутизации. Он связывает URL (`/articles/myNewArticle`) с компонентом страницы.

#### 2. `metadata.ts` — SEO и мета-информация

Содержит заголовок, описание, ключевые слова и информацию для социальных сетей.

```tsx
import { Metadata } from "next";
import { Article } from "@/models";
import { createArticleMetadata } from "@/shared/metadata";
import articleCover from "@/shared/assets/articles/myNewArticle/cover.jpg";

/** Данные о статье */
export const articleInfo: Article = {
    caption: "Заголовок статьи",
    link: "/articles/myNewArticle",
    cover: articleCover,
    description: "Краткое описание статьи для превью",
    publishDate: new Date(2025, 0, 15), // месяц начинается с 0
};

export const metadata: Metadata = createArticleMetadata(
    articleInfo,
    ["ключевое слово 1", "ключевое слово 2"]
);
```

**Зачем нужен:** Определяет метаданные для SEO-оптимизации, Open Graph (превью в соцсетях) и информацию для списка статей.

#### 3. `article.tsx` — содержимое статьи

Основной компонент с текстом, изображениями и структурой статьи.

```tsx
"use client";

import type { FC } from "react";
import TableOfContents from "@/components/tableOfContents";
import ImageWithCaption from "@/components/ImageWithCaption";

import cover from "@/shared/assets/articles/myNewArticle/cover.jpg";

const SECTIONS = {
    INTRO: "intro",
    CONCLUSION: "conclusion",
} as const;

const tocItems = [
    { caption: "1. Введение", elementId: SECTIONS.INTRO },
    { caption: "2. Заключение", elementId: SECTIONS.CONCLUSION },
];

const MyArticle: FC = () => {
    return (
        <TableOfContents items={tocItems}>
            <div className="article-content-wrapper">
                <div className="article-content">
                    <ImageWithCaption
                        image={cover}
                        alt="Обложка статьи"
                        caption="Обложка статьи"
                    />

                    <h2 className="page-title">Заголовок статьи</h2>
                    <p>Вводный текст статьи.</p>

                    <h3 className="title is-3" id={SECTIONS.INTRO}>
                        1. Введение
                    </h3>
                    <p>Содержимое раздела.</p>

                    <h3 className="title is-3" id={SECTIONS.CONCLUSION}>
                        2. Заключение
                    </h3>
                    <p>Заключительный текст.</p>
                </div>
            </div>
        </TableOfContents>
    );
};

export default MyArticle;
```

**Зачем нужен:** Содержит основной контент статьи с разметкой, изображениями, оглавлением и текстом.

**⚠️ Важно:** Директива `"use client"` обязательна в начале файла, так как компонент использует клиентские возможности React (интерактивность, состояние).

#### Дополнительные шаги

1. Добавьте изображения статьи в `src/shared/assets/articles/myNewArticle/`
2. Зарегистрируйте статью в `src/models/article.ts` для отображения в общем списке
3. Перезапустите сервер разработки для применения изменений

### Создание новых страниц

Для добавления новой страницы (например, "Контакты" или "FAQ") создайте подпапку в `src/app/(pages)/` с тремя файлами:

#### Структура папки страницы

```
src/app/(pages)/
└── myNewPage/
    ├── page.tsx
    ├── metadata.ts
    └── content.tsx
```

#### 1. `page.tsx` — точка входа для Next.js

Этот файл экспортирует компонент страницы и метаданные.

```tsx
import Content from "./content";
export { metadata } from "./metadata";

export default function MyNewPage() {
    return <Content />;
}
```

**Зачем нужен:** Next.js использует этот файл для создания маршрута. Страница будет доступна по адресу `/myNewPage`.

#### 2. `metadata.ts` — SEO и мета-информация

Содержит заголовок, описание и ключевые слова для страницы.

```tsx
import type { Metadata } from "next";
import { createCommonMetadata } from "@/shared/metadata";

export const metadata: Metadata = createCommonMetadata(
    "Заголовок страницы",
    "Описание страницы для поисковых систем",
    ["ключевое слово 1", "ключевое слово 2", "ключевое слово 3"]
);
```

**Зачем нужен:** Определяет метаданные для SEO-оптимизации, заголовок вкладки браузера и описание страницы.

#### 3. `content.tsx` — содержимое страницы

Основной компонент с разметкой и контентом страницы.

```tsx
"use client";

export default function MyNewPageContent() {
    return (
        <div className="container p-5">
            <h1 className="title is-2">
                Заголовок страницы
            </h1>
            <p className="content">
                Текст содержимого вашей новой страницы.
            </p>

            <h2 className="title is-3">
                Подзаголовок
            </h2>
            <p className="content">
                Дополнительный контент.
            </p>
        </div>
    );
}
```

**Зачем нужен:** Содержит основную разметку и контент страницы. Использует Bulma-классы для стилизации.

**⚠️ Важно:** Директива `"use client"` обязательна в начале файла для использования клиентских возможностей React.#### Дополнительные шаги

1. Добавьте ссылку на новую страницу в меню навигации (`src/components/sidebar/component/items.ts`)
2. При необходимости добавьте изображения в `src/shared/assets/`
3. Перезапустите сервер разработки для применения изменений
