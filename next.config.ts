/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: "/Rice-And-Stripes",
  assetPrefix: "/Rice-And-Stripes/",
};

export default nextConfig;
